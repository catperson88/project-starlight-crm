import React from 'react';
import Board from './Board';

export const App = (props) => {
  return (
  <div className = "app">
    <main className = "flexbox">
      <Board />
    </main>

  </div>)
}

export default App
